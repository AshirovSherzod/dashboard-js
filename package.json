{
  "name": "dashly-demo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A self-contained customer workspace demo built with native browser APIs.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "test:browser": "node scripts/browser-test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
